---
import { storyblokEditable } from '@storyblok/astro'

const { blok } = Astro.props


---

<div
  {...storyblokEditable(blok)}
  class='w-full p-5 bg-[#c1baf0] rounded-[5px] text-center m-1'
>
  <h3 class='text-2xl text-[#070707] font-bold'>{blok.name}</h3>
</div>
